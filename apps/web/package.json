{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun build src/index.tsx --outdir dist --target bun",
    "dev": "bun run --watch src/index.tsx"
  },
  "dependencies": {
    "hono": "^4.7.4"
  }
}
