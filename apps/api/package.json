{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@islamic-network/prayer-times": "workspace:*",
    "hono": "^4.7.4",
    "zod": "^3.24.2"
  }
}
