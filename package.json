{
  "name": "praytime-monorepo",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "turbo": "^2.4.4",
    "typescript": "^5.7.3"
  }
}
